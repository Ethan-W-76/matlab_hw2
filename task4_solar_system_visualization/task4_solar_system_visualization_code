% Author Name: Ethan Warminsky
% Email: warmin16@rowan.edu 
% Created On: 09/29/2024
% Updated On: 10/11/2024
% Updated By: Ethan Warminsky 
% Course: MATLAB Programming - Fall 2024
% Assignment: Homework 2
% Submission Date: [10/11/24]
% All rights are reserved - GNU License

% Start of the Solar System Visualization Code 

% Vector for the names of each planet (RIP Pluto)
planet_names = {'Mercury', 'Venus', 'Earth', 'Mars', 'Jupiter', 'Saturn', 'Uranus', 'Neptune'};

% Vector for distance of each planet from the sun in AU (Earth = 1) 
planet_distances = [0.39, 0.72, 1.00, 1.52, 5.20, 9.58, 19.2, 30.1]; 

% Vector for the size of each planet relative to Earth (Earth = 1) 
planet_sizes = [0.38, 0.95, 1.00, 0.53, 11.2, 9.45, 4.01, 3.88]; 

% Creation of a Figure 
figure;

% Scatter plot of planet distances vs planet sizes
subplot(1, 2, 1);
scatter(planet_distances, planet_sizes, planet_sizes * 100, 'filled');
title('Planet Sizes vs Distances from Sun');
xlabel('Distance from Sun (AU)');
ylabel('Relative Size (Earth = 1)');
grid on;
set(gca, 'XLim', [0 35]);

% Labels of each Planet 
for i = 1:length(planet_distances)
    text(planet_distances(i) + 0.5, planet_sizes(i), planet_names{i});
end

% Bar chart of planets' distances from the sun
subplot(1, 2, 2);
bar(planet_distances);
title('Planet Distances from the Sun');
xlabel('Planet');
ylabel('Distance from Sun (AU)');
xticklabels(planet_names);
grid on;

% Adjust figure layout and appearance
sgtitle('Solar System Visualization');

% Save as an image file
saveas(gcf, 'solar_system_visualization.png');

% End of the Solar System Visualization Code 